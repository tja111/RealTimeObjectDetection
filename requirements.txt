# Python dependencies for Real-Time Object Detection
# Updated to use patched versions - Python 3.8-3.11 required

# Core dependencies (SECURE VERSIONS - Patched for known vulnerabilities)
tensorflow==2.12.1          # Patched version - fixes 150+ CVEs
opencv-python==4.8.1.78     # Patched version - fixes CVE-2023-4863
numpy>=1.23.0,<2.0.0
matplotlib>=3.3.0
pillow>=8.0.0
lxml>=4.6.0

# Jupyter for running notebooks
jupyter>=1.0.0
ipython>=7.16.0

# Object Detection API dependencies
# These will be installed when you run: python -m pip install . 
# from the tensorflow/models/research directory
# But listing here for reference:
# pycocotools>=2.0.2
# tf-slim>=1.1.0

# Optional but recommended
# pandas>=1.1.0  # For data manipulation
# scikit-learn>=0.23.0  # For additional ML utilities

# LEGACY VERSIONS (Educational Use Only - Contains Vulnerabilities):
# If you need to use the old vulnerable versions for compatibility with old tutorials:
# tensorflow==2.3.1 (Contains 150+ CVEs)
# opencv-python==4.4.0.46 (Contains CVE-2023-4863)
# numpy>=1.19.0,<1.20.0
# Note: Python 3.7-3.8 required for legacy versions
# WARNING: Only use in isolated educational environments

